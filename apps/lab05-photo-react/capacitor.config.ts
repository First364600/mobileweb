import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'lab05-photo-react',
  webDir: '../../docs/lab05-photo-react'
};

export default config;
